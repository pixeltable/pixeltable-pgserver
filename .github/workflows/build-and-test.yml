name: Build and Test
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [created]
  workflow_dispatch:
jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15-intel
            cibw-archs: x86_64
            deployment-target: '10.13'
          - os: macos-15
            cibw-archs: arm64
            deployment-target: '11.0'
          - os: ubuntu-24.04
            cibw-archs: x86_64
            deployment-target: ''
          - os: ubuntu-24.04-arm
            cibw-archs: aarch64
            deployment-target: ''
          - os: windows-2025
            cibw-archs: 'AMD64 ARM64'
            deployment-target: ''
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install cibuildwheel
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade cibuildwheel
      - name: Install flex and bison (Windows)
        # PostgreSQL 18 requires flex + bison to build from the release tarball (unlike
        # <=16, whose tarball ships pre-generated parser/scanner sources). winflexbison
        # provides standalone win_flex/win_bison binaries with no MSYS2 runtime dependency.
        if: ${{ startsWith(matrix.os, 'windows') }}
        run: choco install --no-progress -y winflexbison3
      - name: Restore postgres build from cache
        if: ${{ !startsWith(matrix.os, 'ubuntu') }}
        id: restore-postgres
        uses: actions/cache/restore@v3
        env:
          cache-name: cache-postgres
        with:
          path: |
            pgbuild
            src/pixeltable_pgserver/pginstall
            src/pixeltable_pgserver/pginstall18
          key: ${{ runner.os }}-${{ runner.arch }}-build-${{ env.cache-name }}-${{
            hashFiles('Makefile', 'pgbuild/Makefile', '.github/workflows/build-and-test.yml') }}
      - name: Build postgres and pgvector
        if: ${{ !startsWith(matrix.os, 'ubuntu') && !steps.restore-postgres.outputs.cache-hit }}
        # Run `make` now so that we may cache the postgres build before running cibuildwheel
        # (On Linux cibuildwheel runs in a docker container, so this won't work)
        env:
          MACOSX_DEPLOYMENT_TARGET: ${{ matrix.deployment-target }}
          # Point PG's configure at winflexbison's binaries on Windows; empty elsewhere so
          # Linux/macOS auto-detect their own flex/bison.
          FLEX: ${{ startsWith(matrix.os, 'windows') && 'win_flex' || '' }}
          BISON: ${{ startsWith(matrix.os, 'windows') && 'win_bison' || '' }}
        run: make
      - name: Save postgres build
        if: ${{ !startsWith(matrix.os, 'ubuntu') && !steps.restore-postgres.outputs.cache-hit }}
        id: cache-postgres
        uses: actions/cache/save@v3
        env:
          cache-name: cache-postgres
        with:
          path: |
            pgbuild
            src/pixeltable_pgserver/pginstall
            src/pixeltable_pgserver/pginstall18
          key: ${{ runner.os }}-${{ runner.arch }}-build-${{ env.cache-name }}-${{
            hashFiles('Makefile', 'pgbuild/Makefile', '.github/workflows/build-and-test.yml') }}
      - name: Build wheels
        env:
          CIBW_ARCHS: ${{ matrix.cibw-archs }}
          CIBW_BUILD_VERBOSITY: 1
          CIBW_SKIP: cp314t* *-musllinux*
          MACOSX_DEPLOYMENT_TARGET: ${{ matrix.deployment-target }}
        run: python -m cibuildwheel --output-dir wheelhouse
      - uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions-${{ matrix.os }}
          path: wheelhouse/*.whl
  publish-to-pypi:
    if: ${{ startsWith(github.ref, 'refs/tags/') }}
    name: Publish Python dist to PyPI
    needs:
      - build_wheels
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/pixeltable-pgserver
    permissions:
      id-token: write  # IMPORTANT: mandatory for trusted publishing
    steps:
      - name: Download all the dists
        uses: actions/download-artifact@v4
        with:
          path: dist/
          merge-multiple: true
      - name: Publish distribution 📦 to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
